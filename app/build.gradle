apply plugin: 'application'
mainClassName = 'org.detoeuf.hexagonal.app.BlogApp'

javaModule.name = 'org.detoeuf.hexagonal.app'

dependencies {
    compile project(':domain')
    runtimeOnly project(':facebook-connector')
    runtimeOnly project(':twitter-connector')
    runtimeOnly project(':persistence')
    runtimeOnly project(':webserver')
}
